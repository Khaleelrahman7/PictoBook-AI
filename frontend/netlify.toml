[build]
  command = "npm run build"
  # Don't set publish directory - Netlify Next.js plugin handles this

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Use Netlify's official Next.js plugin for App Router support
[[plugins]]
  package = "@netlify/plugin-nextjs"

